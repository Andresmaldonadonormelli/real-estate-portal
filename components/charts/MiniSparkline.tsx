import type { MonthlyFinancialPoint } from '@/lib/financialHistory';

export default function MiniSparkline({rows,negative=false}:{rows:MonthlyFinancialPoint[];negative?:boolean}){const values=rows.map(row=>row.cashFlow);const maxAbs=Math.max(1,...values.map(value=>Math.abs(value)));const points=values.map((value,index)=>`${2+index*(60/Math.max(1,values.length-1))},${10-(value/maxAbs)*8}`).join(' ');return <svg className={`portfolio-sparkline ${negative?'is-negative':'is-positive'}`} viewBox="0 0 64 20" aria-hidden="true"><line x1="2" x2="62" y1="10" y2="10" className="portfolio-sparkline-zero"/><polyline points={points}/></svg>}
