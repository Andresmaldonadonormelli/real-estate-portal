import { apiError, adminDb, authenticatedUser, decryptToken, plaidClient } from '@/lib/plaid/server';
export const runtime='nodejs';
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await authenticatedUser(request);const {id}=await params;const db=adminDb();const {data:item,error}=await db.from('plaid_items').select('*').eq('id',id).eq('user_id',user.id).single();if(error)throw error;await plaidClient().itemRemove({access_token:decryptToken(item.access_token_encrypted)});await db.from('plaid_items').update({status:'disconnected',disconnected_at:new Date().toISOString(),access_token_encrypted:'revoked'}).eq('id',id);await db.from('plaid_accounts').update({archived_at:new Date().toISOString()}).eq('item_id',id);return Response.json({ok:true});}catch(error){return apiError(error)}}
