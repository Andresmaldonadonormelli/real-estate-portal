import { apiError, adminDb, authenticatedUser } from '@/lib/plaid/server';
import { backfillPlaidAccount, syncPlaidItem } from '@/lib/plaid/sync';
export const runtime='nodejs';
export async function POST(request:Request){try{const user=await authenticatedUser(request);const body=await request.json().catch(()=>({}));const db=adminDb();let query=db.from('plaid_items').select('*').eq('user_id',user.id).is('disconnected_at',null);if(body.itemId)query=query.eq('id',body.itemId);const {data:items,error}=await query;if(error)throw error;const results=[];for(const item of items||[]){results.push(await syncPlaidItem(db,item));const {data:accounts}=await db.from('plaid_accounts').select('id').eq('item_id',item.id).eq('import_enabled',true).not('property_id','is',null);for(const account of accounts||[])await backfillPlaidAccount(db,item,account.id);}return Response.json({ok:true,results});}catch(error){return apiError(error)}}
